2026年7月1日（周三）

本来打算坐头班车的，结果几个闹铃愣是没吵醒，昨晚上的蚊子太厉害了。

和卖电脑的确认了需求，明天早上装机，但是今天的活还是得干。
一整个上午都在对4GB的内存做性能优化，主要是一千多张塞表里之后xlsx文件体积超过了2GB，WPS完全打不开这玩意儿。
画了点功夫把文件体积缩小到了350MB。
还找那卖电脑的借了根8GB DDR3插上凑合使使，总比4GB要好得多。


但是一部分命名格式正确的文件无法被识别，就很怪一堆事情忙活到9点半，只能明天看看

下班前一边和数据中心主管一起对着报表死磕，一边一块儿和老板汇报工作，之后的工作方式也差不多确定了
其他人继续原有的工作模式维持业务运行，我就想办法整点技术玩意儿把她们捞出来
明天也是她任工处理脚本搞不定的部分，以及其他工作，我就专心搞开发。

```Update_log.md
## v1.6 (当前)

**新增**：多 xlsx 批量处理
- 清单中所有 xlsx 逐一独立处理，相同图片集对各文件分别匹配、分别插入
- 输出对应保存为 `输出/<原文件名>.xlsx`
- CSV 文件名含对应 xlsx 名称（如 `未处理记录_邛崃超市_20260701120000.csv`），避免覆盖
- 运行结束打印总计统计

---


## v1.5

**新增**：文件名特殊符号自动过滤
- 解析前清除非汉字/字母/数字/小数点的特殊符号（替换为空格）
- 兼容 `【条码】价格-类型（渠道）.jpg` 等带符号的文件名
- 解析前清除非汉字/字母/数字/小数点的特殊符号

**移除**：未处理图片类型分类保存（简化输出结构）

---

## v1.4.4

**重构**：纯条码匹配，忽略商品类型
- `find_type_barcode_row` → `find_barcode_row`：仅匹配条码列，不再校验 B 列类型
- `parse_filename` 改为按条码边界切分：type 节可选、缺失时标记"未知类型"
- 兼容缺价格的节（跳过不中断）和缺类型的节（自动补"未知类型"）
- **影响**：文件名不再需要严格保持 `条码 价格 类型` 三元组结构

---

## v1.4.3.2

**新增**：图片压缩
- 插入前边长 > 800px 等比缩放，转 JPEG quality=85
- Pillow 未安装时降级为原图

---

## v1.4.3_1

**新增**：遗漏条目报告
- 输出文件夹中额外生成 `[原始文件名] 未找到照片的条目.xlsx`
- 保留原始表头，仅含本次运行未匹配到任何图片的数据行
- 从下往上删除已命中行，避免行号偏移

---

## v1.4.3

**新增**：未处理图片按商品类型分文件夹
- 输出目录结构变为 `未处理图片/未成功导入图片 <类型名>/`
- 类型推断：优先匹配 TYPE_NAME_MAP 全称，其次简称，均不中归入 `未知类型`
- CSV 记录新增「商品类型」列

---

## v1.4

**重构**：单工作表 + B 列类型匹配
- **重大架构变更**：不再按商品类型拆分多个工作表，所有数据集中在单一工作表中
- 商品类型通过 B 列的值区分（列字母可配置），与 `[types]` 映射目标值做精确匹配
- 条码匹配条件升级为双重匹配：B列类型 + 条码列 **同时** 匹配才视为找到
- 新增 `config.ini` → `[workbook]` 配置节：
  - `sheet_name` — 目标工作表名（留空则使用第一个工作表）
  - `type_column` — 商品类型所在列（默认 B）
- 弃用 `[sheet_start_rows]` 配置节（单工作表统一使用 `start_row`）
- `load_existing_images()` 仅扫描目标工作表（不再遍历所有工作表）

**修复**：排除 Excel 临时锁文件 `~$`
- 查找 xlsx 时自动过滤 `~$` 开头的临时文件，避免 `PermissionError`

**新增**：渠道无照片列兼容
- 当某渠道只配置 `_price` 无 `_image` 时，仅写价格、跳过图片插入

---

## v1.3

**新增**：未处理图片自动归档
- 未能插入 xlsx 的图片（所有条目均匹配失败，或文件名解析失败）自动拷贝到 `输出/未处理图片/` 目录
- 运行结束时打印未处理图片清单，便于人工复核补录

**新增**：同单元格图片去重
- 当多张图片因条码相同而竞争同一单元格时，先到先得，后续图片跳过插入
- 被跳过的图片自动拷贝到 `输出/重复图片/` 目录
- 以 `(工作表名, 行号, 列号)` 三元组唯一标识单元格，跨工作表/跨渠道互不干扰
- 启动时自动扫描 xlsx 中已有图片并预填充占用表，支持分批次增量处理同一文件
- 出现冲突/未处理图片时自动生成 CSV 记录：
  - `输出/重复图片/冲突记录[yyyyMMddhhmmss].csv` — 冲突单元格地址 + 冲突图片文件名
  - `输出/未处理图片/未处理记录[yyyyMMddhhmmss].csv` — 未处理图片文件名
- 无对应事件时不生成 CSV

---
```
